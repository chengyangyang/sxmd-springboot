

# cassandra 设置
cassandra:
  cluster-name: test-cluster
  keyspace-name: cassandra_test
  contact-points: localhost
  port: 9042
  username: cassandra
  password: cassandra

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mytest?autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=UTC&autoReconnect=true
    username: root
    password: admin









